/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TracksPipe } from './tracks.pipe';

describe('Pipe: Tracks', () => {
  it('create an instance', () => {
    let pipe = new TracksPipe();
    expect(pipe).toBeTruthy();
  });
});
